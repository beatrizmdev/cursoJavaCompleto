package entities;

public class SavingAccountsPlus extends SavingsAccount {
	/*
	// ERRO: sobrepondo uma 2a vez mas o anterior tem a keyword "final" (na SavingsAccount)	
	@Override
	public void withdraw(double amount) { // reaproveitando o método mas retirando coisa dele / mudando ele
		balance -= amount + 2;
		*/
}
